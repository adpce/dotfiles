#!/bin/fish

set -x XDG_CONFIG_HOME "$HOME/.config"
set -x XDG_DATA_HOME "$HOME/.local/share"
set -x EDITOR "$HOME/neovim/bin/nvim"
set -x MANPAGER "$HOME/neovim/bin/nvim +Man!"
set -x BAT_THEME 'ansi'
set -x PLAN9 "$HOME/Downloads/prog/plan9port"
set -x PF_INFO "ascii os kernel wm editor shell uptime pkgs memory palette"
